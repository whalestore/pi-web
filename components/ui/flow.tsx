"use client";

/**
 * ui/flow —— 流程图节点（kumo 标准）
 *
 * @example
 * <Flow>...</Flow>
 */
import { Flow as KumoFlow } from "@cloudflare/kumo/components/flow";

export const Flow = KumoFlow;
export default Flow;
