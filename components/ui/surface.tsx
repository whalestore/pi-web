"use client";

/**
 * ui/surface —— 面板容器（kumo 标准）
 *
 * @example
 * <Surface variant="elevated" className="p-4">面板内容</Surface>
 */
import { Surface as KumoSurface } from "@cloudflare/kumo/components/surface";

export const Surface = KumoSurface;
export default Surface;
