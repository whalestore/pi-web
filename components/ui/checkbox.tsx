"use client";

/**
 * ui/checkbox —— 复选框（kumo 标准）
 *
 * @example
 * <Checkbox label="启用" checked={on} onCheckedChange={setOn} />
 */
import { Checkbox as KumoCheckbox } from "@cloudflare/kumo/components/checkbox";

export const Checkbox = KumoCheckbox;
export default Checkbox;
