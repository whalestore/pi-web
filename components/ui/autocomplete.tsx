"use client";

/**
 * ui/autocomplete —— 自动补全输入框（kumo 标准）
 *
 * @example
 * <Autocomplete items={[{value:"a",label:"A"}]} />
 */
import { Autocomplete as KumoAutocomplete } from "@cloudflare/kumo/components/autocomplete";

export const Autocomplete = KumoAutocomplete;
export default Autocomplete;
