"use client";

/**
 * ui/combobox —— 组合框（搜索+选择）（kumo 标准）
 *
 * @example
 * <Combobox items={[{value:"a",label:"A"}]} />
 */
import { Combobox as KumoCombobox } from "@cloudflare/kumo/components/combobox";

export const Combobox = KumoCombobox;
export default Combobox;
