"use client";

/**
 * ui/clipboard-text —— 复制文本按钮（kumo 标准）
 *
 * @example
 * <ClipboardText text="要复制的文本" />
 */
import { ClipboardText as KumoClipboardText } from "@cloudflare/kumo/components/clipboard-text";

export const ClipboardText = KumoClipboardText;
export default ClipboardText;
