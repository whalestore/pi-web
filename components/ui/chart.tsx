"use client";

/**
 * ui/chart —— 图表（需 echarts 依赖）（kumo 标准）
 *
 * @example
 * <TimeseriesChart data={...} />
 */
import { TimeseriesChart as KumoTimeseriesChart } from "@cloudflare/kumo/components/chart";

export const TimeseriesChart = KumoTimeseriesChart;
export default TimeseriesChart;
