"use client";

/**
 * ui/menubar —— 菜单栏（kumo 标准）
 *
 * @example
 * <MenuBar items={[...]} />
 */
import { MenuBar as KumoMenuBar } from "@cloudflare/kumo/components/menubar";

export const MenuBar = KumoMenuBar;
export default MenuBar;
