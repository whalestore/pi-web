"use client";

/**
 * ui/layer-card —— 分层卡片容器（kumo 标准）
 *
 * @example
 * <LayerCard className="p-4">内容</LayerCard>
 */
import { LayerCard as KumoLayerCard } from "@cloudflare/kumo/components/layer-card";

export const LayerCard = KumoLayerCard;
export default LayerCard;
