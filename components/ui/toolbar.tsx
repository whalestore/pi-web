"use client";

/**
 * ui/toolbar —— 工具栏容器（kumo 标准）
 *
 * @example
 * <Toolbar><Button variant="ghost">工具</Button></Toolbar>
 */
import { Toolbar as KumoToolbar } from "@cloudflare/kumo/components/toolbar";

export const Toolbar = KumoToolbar;
export default Toolbar;
