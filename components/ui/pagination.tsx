"use client";

/**
 * ui/pagination —— 分页（kumo 标准）
 *
 * @example
 * <Pagination page={1} pageCount={10} onPageChange={...} />
 */
import { Pagination as KumoPagination } from "@cloudflare/kumo/components/pagination";

export const Pagination = KumoPagination;
export default Pagination;
