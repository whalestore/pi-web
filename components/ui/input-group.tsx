"use client";

/**
 * ui/input-group —— 输入组（前缀/后缀）（kumo 标准）
 *
 * @example
 * <InputGroup leading={<span>https://</span>}><Input /></InputGroup>
 */
import { InputGroup as KumoInputGroup } from "@cloudflare/kumo/components/input-group";

export const InputGroup = KumoInputGroup;
export default InputGroup;
