"use client";

/**
 * ui/date-picker —— 日期选择器（kumo 标准）
 *
 * @example
 * <DatePicker value={date} onChange={...} />
 */
import { DatePicker as KumoDatePicker } from "@cloudflare/kumo/components/date-picker";

export const DatePicker = KumoDatePicker;
export default DatePicker;
