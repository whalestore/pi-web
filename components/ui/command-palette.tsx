"use client";

/**
 * ui/command-palette —— 命令面板（Cmd+K）（kumo 标准）
 *
 * @example
 * <CommandPalette open={open} onOpenChange={...} />
 */
import { CommandPalette as KumoCommandPalette } from "@cloudflare/kumo/components/command-palette";

export const CommandPalette = KumoCommandPalette;
export default CommandPalette;
